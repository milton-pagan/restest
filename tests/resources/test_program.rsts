
(url "http://localhost:5000")
(header ["Content-Type":"application/json", "Accepts":"*/*"])

(proc givemeaone[]:
    return 1
) 

(test on "/disasterStorage/products" test1:
    define product get()
    define payload {"product_name": "water jug",
        "product_description": "jug",
        "product_price": 1.0,
        "product_quantity": 100,
        "latitude": 50,
        "longitude": 100.5,
        "category": "water", 
        "category_attributes": {
                                "water_exp_date": "2020-05-20", 
                                "water_volume_ml": 1500
                                }
        }
    define temp givemeaone()
    print(temp)
    print(payload)
    post(payload)
)

(test on "/disasterStorage/products/{id}" test2:
    delete(id<<42)
)